Upstream GraphST example data (e.g. `151673/`). Not used by the SARSIM pipeline, which resolves paths from `../MDACC Data` spreadsheets. Safe to remove to reduce repo size if you do not need it for GraphST demos.
